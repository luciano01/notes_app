name: notes_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  get: ^4.6.5
  path_provider: ^2.0.15
  intl: ^0.18.1
  google_fonts: ^5.1.0

  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  mocktail: ^0.3.0
  hive_generator: ^2.0.0
  build_runner: ^2.4.6

  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/core/ui/assets/images/
